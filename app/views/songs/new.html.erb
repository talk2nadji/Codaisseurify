<h1>Create new song for <%= @artist.name %></h1>

<%= form_for([@artist, @song]) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :time %>
<%= f.number_field :time %>

<%= f.submit "Create" %>
<% end %>
